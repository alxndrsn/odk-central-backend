on: [push]

jobs:
  build_api_docs:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build API Docs
        uses: ./
        id: build-api-docs
